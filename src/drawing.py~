import sys
import numpy as np

class drawing(object):
    def __init__(self, d_type, d_no, d_array, d_w=28, d_h=28):
        self.d_w = d_w
        self.d_h = d_h
        self.d_type = d_type
        self.d_no = d_no
        self.d_array = np.reshape(d_array, (d_h, d_w))
